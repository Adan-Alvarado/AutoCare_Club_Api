info:
  name: Get All
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5172/api/appointments
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
