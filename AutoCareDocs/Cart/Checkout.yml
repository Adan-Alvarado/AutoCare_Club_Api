info:
  name: Checkout
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/cart/checkout"
  body:
    type: json
    data: |-
      {
        "vehicleId": "",
        "appointmentId": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
