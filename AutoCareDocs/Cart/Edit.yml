info:
  name: Edit
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/api/cart/items/{id}"
  body:
    type: json
    data: |-
      {
        "quantity": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
