info:
  name: Get Order by Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/orders/be328927-c32d-4f82-9fa6-99a198dda6db"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
