info:
  name: Get by Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/role/{id}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
