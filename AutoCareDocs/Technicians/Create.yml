info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/technicians"
  body:
    type: json
    data: |-
      {
        "userId": "aa633402-6e61-4df8-a62f-e1a617bb4fae",
        "specialty": "MECANICA GENERAL"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
