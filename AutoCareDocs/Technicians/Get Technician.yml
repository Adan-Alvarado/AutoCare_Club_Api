info:
  name: Get Technician
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/technicians"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
