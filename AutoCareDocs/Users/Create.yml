info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/users"
  body:
    type: json
    data: |-
      {
        "firstName": "prueba",
        "lastName": "t",
        "email": "pt@gmail.com",
        "roles": [
          "Customer"
        ],
        "password": "Joseph1234!",
        "confirmPassword": "Joseph1234!"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
