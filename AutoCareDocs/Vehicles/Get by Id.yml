info:
  name: Get by Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/vehicles/be328927-c32d-4f82-9fa6-99a198dda6db"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
